// import { useSelector } from 'react-redux';

const Main = () => {
    // const user = useSelector(state => state.user);
    return (
    <>
        <div>Main page</div>
    </>);
};

export default Main;